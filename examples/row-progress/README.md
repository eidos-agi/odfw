# row-progress fixture

Public fictional pack fragment for `odwf-validate --progress` (SPEC §8a.6).

- Full two-month window in `evidence/demo_metric-progress.json` **passes** progress fairness.
- Not a full warehouse face — use with `--progress` only (not `--strict` lifecycle).

```bash
python3 -m odwf.validate --progress examples/row-progress
```
